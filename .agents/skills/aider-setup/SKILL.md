---
name: aider-setup
description: 'Set up and maintain the aider nightly local-coding pipeline: two local Ollama models (a writer that codes and tests, a reviewer that never edits) run one aider process per plan overnight, with no Claude Code involved, driven by aider-plan.ps1/aider-night.ps1 and gated by measured token budgets.'
---

Pointer mirror generated by `install.ps1`. The authoritative skill, with every
instruction, script and reference it ships, lives at
`.claude/skills/aider-setup/SKILL.md` in this repository.

MANDATORY FIRST STEP: read `.claude/skills/aider-setup/SKILL.md` in full and follow
it exactly. Do not act from this pointer alone - it carries the trigger description
and nothing else.
