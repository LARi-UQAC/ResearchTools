# Dependencies for the uqac-forms skill scripts.
#
# pdf_ingest.py : requests (validated HTTPS download)
#
# Later units add their own, and the licence constraint decides which:
#   field_map.py / fill_form.py : pypdf   (BSD-3, RT-2 and RT-3)
#   sign_form.py                : pyhanko (MIT, RT-4)
# This skill must stay AGPL-free because it ships inside a deployable container.
# PyMuPDF, used by extract-statistic, is AGPL-3.0 and stays isolated in that
# skill.
#
# Pinned exactly, then audited before use:
#   pip-audit -r .claude/skills/uqac-forms/requirements.txt --strict
requests==2.34.2
